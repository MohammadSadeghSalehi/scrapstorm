# C2 restore — missions

The ablated tree is this repo with `src/game/missions/` emptied. Restore the
career ladder: fifteen rivals, objectives that can complete inside
`GameSimulation` without a renderer, and a payout that cannot dead-end a broke
player.

Oracle: `node scripts/mission-smoke.mjs` exits 0.
